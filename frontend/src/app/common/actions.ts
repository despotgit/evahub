import { Action } from "@ngrx/store";

export const homeScore = createAction("[Scoreboard Page] Home Score");

export class Login implements Action {
    readonly type = "[Reports Page] ReportSelected";
}
